{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "output": "${workspaceFolder}/build/GeareoSaveTool.exe",
            "console": "integratedTerminal"
        }
    ]
}